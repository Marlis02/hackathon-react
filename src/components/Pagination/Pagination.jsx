import React from "react";
import { Button } from "react-bootstrap";

const Pagination = () => {
  return (
    <div>
      <Button>prev</Button>
      <Button>next</Button>
    </div>
  );
};

export default Pagination;
